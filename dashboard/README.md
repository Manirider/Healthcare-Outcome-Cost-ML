# Dashboard

Streamlit-based interactive dashboard for the healthcare outcome prediction model.

## Usage

```bash
streamlit run app.py
```

Run from the project root directory. The dashboard loads the trained model and provides:
- Patient risk scoring with adjustable clinical inputs
- Population-level analytics
- Cost-effectiveness exploration (ICER, NMB)
- Sensitivity analysis with adjustable cost assumptions
